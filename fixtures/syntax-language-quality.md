## JSON

```json
{"name": "Moonmark", "ready": true, "images": 250, "note": null}
```

## PowerShell

```powershell
$name = "Moonmark"
Write-Output -InputObject $name # Native viewer
```

## Python

```python
def greet(name: str, count: int = 2):
    # Keep the output calm.
    return f"Hello {name}" * count
```
